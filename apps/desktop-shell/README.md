# Legacy Tauri Prototype

This folder contains the earlier Tauri-based prototype for Desktop Companion.

It is kept for reference because it captures an earlier cross-platform shell experiment and a TypeScript package split that may still be useful when comparing host strategies.

Current status:

- not the canonical runtime
- not the primary place for new product work
- kept as a reference implementation only

The current product path is:

- `../../DesktopCompanion.Windows.sln`
- `../desktop-shell-wpf`
